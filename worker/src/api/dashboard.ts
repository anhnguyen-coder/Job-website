import { ENDPOINT } from "@/constant/constant";

const GET = {
  GET_DASHBOARD_STATS: `${ENDPOINT.WORKER_ENPOINT}/dashboard/stats`,
  PAYMENT_HISTORY: `${ENDPOINT.WORKER_ENPOINT}/payment/history`,
};

export { GET };
